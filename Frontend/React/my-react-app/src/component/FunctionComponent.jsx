// 함수형 컴포넌트
function MyComponentApp() {
  return <> </>;
}

// 함수 표현식으로 변경
const MyExpressionApp = function () {
  return <> </>;
};

// 화살표 함수 변경
const MyArrowApp = () => {
  return <> </>;
};

// 컴포넌트 내보내기
export { MyComponentApp, MyExpressionApp, MyArrowApp };
