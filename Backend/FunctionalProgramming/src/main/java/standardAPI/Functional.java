package standardAPI;

// java.util.function ⇒ 함수형 인터페이스를 제공

// Consumer	⇒ void accept(T t);	        ⇒ 매개 변수는 있고, 리턴값은 없음
// Supplier	⇒ T get();		            ⇒ 매개 변수는 없고, 리턴값은 있음
// Function	⇒ R apply(T t);	            ⇒ 매개 변수도 있고, 리턴값도 있음
// 					                      주로 매개값을 리턴값으로 맵핑 (타입 변환)
// xxxOperator	⇒ applyAs...            ⇒ 매개 변수도 있고, 리턴값도 있음
// 					                      주로 매개값을 연산하고 결과를 반환
// Predicate	⇒ boolean test(T t);	⇒ 매개 변수도 있고, boolean을 반환
// 					                      매개값을 조사해서 true/false를 반환

public class Functional {
}
